import array as arr 
a=arr.array("i",eval(input("enter the array")))
pos=int(input("enter the position you want to delete the number"))
for i in range(pos-1,len(a)-1):
    a[i]=a[i+1]
a.pop(len(a)-1)
print(a)
