import array as arr 
def section(a):
    for i in range(0,len(a)-1):
        for j in range(i+1,len(a)-1):
            if a[j]<a[i]:
                temp=a[i]
                a[i]=a[j]
                a[j]=temp
a=arr.array("i",eval(input("enter the array")))
section(a)
print(a)
