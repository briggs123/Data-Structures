import array as arr 
a=arr.array("i",eval(input("enter the array")))
f=int(input("enter the number you want to insert"))
pos=int(input("enter the position you want to insert the number"))
for i in range(len(a)-1,pos-1):
    a[i+1]=a[i]
a[pos-1]=f 
print(a)
